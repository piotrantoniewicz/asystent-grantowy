# Stan projektu — Asystent Wniosków Grantowych

Aktualizuj po każdym etapie — wpisz datę i co ukończono. Plan etapów: `dokumentacja-aplikacja-granty/11-plan-pracy.md`.

- [x] Etap 1 — szkielet + baza (2026-07-11)
- [x] Etap 2 — logowanie (2026-07-11, wymaga RESEND_API_KEY do pełnego testu wysyłki maila)
- [x] Etap 3 — czat + router AI (2026-07-11, przetestowano na żywo: SIMPLE→Haiku, COMPLEX→Sonnet, odmowa poza zakresem, blokada po limicie darmowych pytań; poprawiono błędny ID modelu Sonnet w dokumentacji, `claude-sonnet-5-0` → `claude-sonnet-5`)
- [x] Etap 3.5 — poprawki i zabezpieczenia (2026-07-13, instrukcja: `dokumentacja-aplikacja-granty/12-etap-3-5-poprawki.md`; przejście na PostgreSQL/Neon zrobione i przetestowane na żywo — logowanie, czat, rate limit 4/min, limit długości wiadomości, blokada bez ciasteczka `ag_device`, wyczerpanie darmowej puli, usuwanie rozmowy, licznik pytań po `/api/me`; `npm test` — 9/9 zielone; `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Etap 4 — scraping (2026-07-13, `src/lib/scraper/` — ochrona SSRF z rozwiązywaniem DNS i sprawdzaniem po przekierowaniach, grzeczny fetch (2 równoległe, 300 ms/domenę, timeout 10 s, robots.txt dla podstron), ekstrakcja HTML przez cheerio i PDF przez unpdf, crawler wg limitów z 06-scraping.md (org: 15 podstron/gł.2, konkurs: 20 HTML + 10 PDF, budżet ~100k tokenów z przycinaniem), podsumowanie Haiku; `POST /api/scrape` streamuje NDJSON (`maxDuration=300`), `GET /api/scrape/[sourceId]`; kontekst czatu dołącza zeskrapowane treści z `cache_control` w bloku system wg kolejności z 05-router-ai.md; UI: link w polu wiadomości → wybór organizacja/konkurs → pasek postępu → podsumowanie jako karta w osi czasu rozmowy; przetestowano na żywo na niw.gov.pl (HTML+PDF+wykrywanie skanów) i ngo.pl (podsumowanie AI); `npm test` — 18/18 zielone, `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Etap 5 — płatności (2026-07-15, Stripe Checkout wg `08-platnosci.md`: `src/lib/stripe/packages.ts` (3 pakiety), `POST /api/checkout` (tworzy `Purchase(pending)` + sesję Stripe, cena tylko z serwera), `POST /api/stripe/webhook` (weryfikacja podpisu, `applyCheckoutSessionCompleted` w `src/lib/stripe/webhook.ts` dolicza pytania idempotentnie po statusie `Purchase`), strona `/pakiety` + link z komunikatu o wyczerpanym limicie w czacie; przetestowano na żywo przez Stripe CLI (`stripe listen`) i kartę testową 4242 — zakup oznaczony `paid`, `paidQuestionsRemaining` doliczone; test automatyczny idempotencji webhooka (dwukrotne wywołanie tego samego zdarzenia dolicza raz) na prawdziwej bazie Neon; `npm test` — 19/19 zielone; `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Etap 6 — wygląd (2026-07-15, brak dostarczonego pliku graficznego — właściciel poprosił o sensowny domyślny wygląd; wybrany kierunek: ciepły/społeczny, zieleń szmaragdowa jako kolor główny, kremowe tło, zaokrąglone karty, font Plus Jakarta Sans; zmiany w `globals.css` (paleta jako zmienne CSS z wariantem dark mode), `layout.tsx`, `logowanie/page.tsx`, `logowanie/sprawdz-skrzynke/page.tsx`, `pakiety/page.tsx`, `BuyButton.tsx`, `page.tsx` (nagłówek), `ChatApp.tsx`; przy okazji poprawiono zagnieżdżone podwójne `h-screen` powodujące niepotrzebne przewinięcie panelu bocznego oraz zawijanie nagłówka na telefonie; przetestowano na żywo w przeglądarce (Playwright, sesja testowa w bazie): logowanie, czat z prawdziwą odpowiedzią AI, /pakiety, oraz widok mobilny (390px) wszystkich stron; `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Etap 7 — panel admina (2026-07-15, wg `09-panel-admina.md`; `/admin` dostępny wyłącznie dla e-maili z `ADMIN_EMAILS` — reszta i niezalogowani dostają 404 na stronach i na `/api/admin/*`; zakładki: Pulpit (`GET /api/admin/stats` — użytkownicy, pytania, przychód, szacowany koszt AI w USD z cennika modeli i realnym rozliczeniem cache — dodano do `Message` kolumny `cacheCreationInputTokens`/`cacheReadInputTokens`, zapisywane od teraz przy każdej odpowiedzi, oraz wykres pytań dziennie z 30 dni), Ustawienia (`GET`/`PUT /api/admin/settings` — prompt systemowy z przyciskiem „Przywróć domyślny" i limit darmowych pytań, działa od kolejnego pytania bez restartu), Rozmowy (lista stronicowana z filtrem po e-mailu, podgląd pojedynczej rozmowy tylko do odczytu), Użytkownicy (lista z licznikami i formularz ręcznej korekty płatnych pytań, log w konsoli serwera); przetestowano na żywo przez curl z sesją wstrzykniętą bezpośrednio do bazy (magic link ominięty tylko do testu) na prawdziwych danych: statystyki zgadzają się z bazą, zapis i przywrócenie ustawień, korekta pytań +5/-5, podgląd rozmowy, oraz potwierdzone 404 dla konta bez uprawnień na stronie i w API; dane testowe posprzątane po teście; `npm test` — 19/19 zielone, `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Etap 8 — strony prawne (2026-07-16, wg `10-prawo-rodo.md`; treści w Markdown w `content/prawne/` (polityka-prywatnosci, regulamin, cookies), renderowane przez `react-markdown`+`remark-gfm` na stronach `/polityka-prywatnosci`, `/regulamin`, `/cookies`; dane administratora (imię/nazwa, adres, e-mail) i dostawca hostingu są placeholderami `[UZUPEŁNIĆ: ...]` — właściciel uzupełni przed startem produkcyjnym, zalecany też przegląd prawnika; stopka z linkami do trzech stron na każdej podstronie poza `/admin` (panel wewnętrzny, pominięty za zgodą właściciela); prosty baner cookies (informacyjny, bez zgód, zapamiętywany w localStorage), pomijany w `/admin`; strona logowania — tekst z linkami do Regulaminu i Polityki prywatności; strona `/pakiety` — checkbox zgody na natychmiastowe świadczenie usługi (wymagany, blokuje przycisk „Kup" do zaznaczenia); ostrzeżenie pod oknem czatu o niewpisywaniu danych osobowych osób trzecich i weryfikacji odpowiedzi AI; dostosowano wysokości stron (`h-screen`→`h-full`/`min-h-screen`→`min-h-full`) tak, by stopka mieściła się bez podwójnego przewijania czatu; `npm test` — 19/19 zielone, `tsc --noEmit` i `npm run lint` bez błędów)
- [x] Audyt i poprawki P1–P17 (2026-07-16, instrukcja: `dokumentacja-aplikacja-granty/13-audyt-poprawki.md`; **krytyczne**: webhook Stripe dolicza pytania dopiero przy `payment_status: "paid"` i obsługuje `async_payment_succeeded`/`async_payment_failed`/`expired` (płatności odroczone typu Przelewy24 — **wymaga dopisania tych zdarzeń w panelu Stripe**), limity scrapowania (5 źródeł/rozmowę, 10/godzinę per użytkownik), cennik Sonneta 5 poprawiony z 5/25 na 3/15 USD za MTok (w kodzie był cennik Opusa — zawyżał koszt AI w panelu ~67%), `safeFetch` czyta treść strumieniowo z limitem w trakcie transferu i timeoutem obejmującym body (`SafeFetchResult.body`: `ArrayBuffer`→`Uint8Array`, `extractPdfText` przyjmuje `Uint8Array`); **koszty/poprawność**: twardy budżet 600k znaków zeskrapowanego kontekstu, `cache_control` na ostatniej wiadomości użytkownika (cache historii), `reserveQuestion` rzuca dalej błędy inne niż wyczerpanie limitu (koniec cichego sięgania do puli płatnej przy awarii bazy) + obsługa w `/api/chat` (500 zamiast pobrania pytania), rozszerzona blokada SSRF (CGNAT, TEST-NET, multicast, reserved, ogólne mapowanie `::ffff:` na reguły IPv4; DNS-rebinding świadomie zaakceptowany — komentarz w `ssrf.ts`), `maxDuration=300` w `/api/chat`, atomowa korekta pytań admina (transakcja `Serializable`), `upsert` w `getOrSeedSetting`, guard na brak `AUTH_SECRET`, limit 10 maili logowania dziennie na adres (tabela `FreeQuota`, klucz `login:<hash>:<dzień>`), bezpieczne parsowanie NDJSON w `handleScrape`; **UI**: wysuwany panel boczny na telefonie (`☰ Menu`, nakładka, zamykanie po wyborze rozmowy) i renderowanie Markdownu w odpowiedziach asystenta przez `react-markdown`+`remark-gfm`; **odstępstwa od instrukcji**: w P4 `clearTimeout` w `finally` wokół całej próby (instrukcja kasowała timeout tylko przy sprawdzaniu rozmiaru — zostawałby wiszący timer przy przekierowaniu i błędzie HTTP), w P9 dodany reset pozycjonowania na `sm:` (panel otwarty na wąskim oknie nie zostaje nakładką po poszerzeniu); **P12**: `webhook.test.ts` ma teraz guard wymagający `DATABASE_URL` z „test" w nazwie — `npm test` będzie padać do czasu utworzenia testowego brancha w Neon; `tsc --noEmit` i `npm run lint` bez błędów, testy niezależne od bazy (ssrf, html, quota) 18/18 zielone, `npm test` **nieuruchamiany** (guard z P12); P9 i P10 zweryfikowane w przeglądarce przez właściciela (menu wysuwane na wąskim ekranie działa, listy i pogrubienia renderują się poprawnie) — asystent nie mógł tego sprawdzić sam, bo panel czatu jest za logowaniem przez link mailowy)
- [x] Poprawki UX P1 i P2 z `14-ux-bez-tarcia.md` (2026-07-17; Partia 1 — ślepe zaułki: U1 `normalizeUrlInput` w `ssrf.ts` uzupełnia brakujące `https://` przed `assertSafeUrl` (testy dla wariantów bez protokołu, z protokołem, z białymi znakami i `javascript:`), U2 komunikaty błędu scrapingu (serwer i UI) proszą o link do dokumentu zamiast wklejania treści regulaminu, U3 `looksLikeBareUrl` w `ChatApp.tsx` — link wklejony samodzielnie w czacie omija `/api/chat` i trafia do `handleScrape` (automatycznie jako konkurs, gdy w rozmowie jest już organizacja; w przeciwnym razie dwa przyciski wyboru rodzaju źródła zamiast zgadywania), U4 formularze źródeł wydzielone do `SourceForms` i przeniesione z panelu bocznego (niewidocznego na telefonie) do głównej kolumny w pustym stanie rozmowy; Partia 2 — trafność scrapingu: U5 PDF-y w `crawl.ts` dzielone na pasujące/niepasujące do `GRANT_KEYWORDS`, pasujące pobierane w pierwszej kolejności, U6 `pickContentRoot` w `html.ts` czyta treść z `main`/`[role=main]`/`article` (próg 200 znaków, inaczej `body`) zamiast całego `body` (nowe testy), U7 `SCRAPE_SUMMARY_PROMPT` porównuje znalezione dokumenty konkursu z zamkniętą listą typowego kompletu i nazywa braki, usunięte pytanie do użytkownika „Czy czegoś brakuje?"; `tsc --noEmit` i `npm run lint` bez błędów; `npm test` — 25/25 zielone (test webhooka nadal pominięty guardem z P12, brak testowej bazy); Partia 3 ukończona (U8–U11): U8 automatyczny tytuł rozmowy — w `scrape/route.ts` po udanym scrapowaniu konkursu ustawiany tytuł z tytułu strony głównej konkursu (i nazwy organizacji po myślniku, jeśli już zeskrapowana), tylko gdy tytuł to nadal „Nowa rozmowa"; w `chat/route.ts` ustawianie tytułu z pierwszej wiadomości też ograniczone tym samym warunkiem, żeby nie nadpisywało tytułu z konkursu. U9 lista organizacji — nowa tabela `UserOrganization` (`@@unique([userId, rootUrl])`, migracja `20260717092310_add_user_organization` uruchomiona na lokalnej bazie Neon), zapis przez `upsert` po udanym scrapowaniu organizacji (nazwa z tytułu strony głównej, fallback na hostname), `GET /api/organizations`; w pustym stanie rozmowy: 0 zapamiętanych → pole jak dawniej, 1 → adres podstawiany i analiza startuje automatycznie (zero kliknięć), 2+ → przyciski z nazwami organizacji + „inna organizacja" odsłaniające pole. U10 — scraping organizacji domyślnie kopiuje ostatnie udane pobranie tego samego adresu (ten sam użytkownik) zamiast crawlować ponownie; strona konkursu zawsze crawlowana od nowa; przycisk „odśwież" przy karcie źródła organizacji wymusza pełny crawling (`forceRefresh: true`). U11 — `isScraping`/`scrapeProgress` rozbite na stan per rodzaj źródła (`Record<"organization"|"grant", …>`) w `ChatApp.tsx`, formularz organizacji i konkursu blokują się niezależnie, dwa osobne paski postępu. `tsc --noEmit`, `npm run lint`, `npm run build` i `npm test` (25/25, webhook nadal pominięty guardem z P12) bez błędów. Nie przetestowano na żywo w przeglądarce (sesja wymaga logowania przez magic link) — zalecana weryfikacja właściciela: kilka organizacji na koncie, przycisk „odśwież", równoległe wklejanie dwóch linków)
- [x] Rebranding „dobryai.pl" (2026-07-17, wykonane przez właściciela; udokumentowane tu dla ciągłości `STATUS.md`; zastępuje wygląd z Etapu 6): nowa paleta w `globals.css` — ciepłe kremy/brąz zamiast zieleni szmaragdowej (`--primary` to teraz ciemny brąz `#2c1810`, `--primary-hover` zielony akcent `#3d6b52`), usunięty wariant `prefers-color-scheme: dark` (na razie tylko jasny motyw); fonty zmienione z Plus Jakarta Sans na DM Sans (`--font-sans`) + DM Serif Display jako nowy `--font-serif` do nagłówków; nowy komponent `src/components/layout/Brand.tsx` (wordmark „dobry**ai**.pl" linkujący do dobryai.pl) użyty w nagłówku czatu, na `/logowanie`, `/logowanie/sprawdz-skrzynke`, `/pakiety` i w `LegalPageShell`; stopka (`Footer.tsx`) przeprojektowana na ciemny pasek z wordmarkiem zamiast samych linków; promień zaokrągleń zmniejszony z `rounded-2xl`/`rounded-lg` do `rounded`/`rounded-full` konsekwentnie w panelu admina, formularzach, `CookieBanner`, `BuyButton`, kartach pakietów i treści prawnej (nagłówki `h1`/`h2` w `LegalContent` teraz `font-serif`); zmiany czysto wizualne, bez ruszania logiki — `tsc --noEmit` przechodzi bez błędów po tych zmianach; **nieprzetestowane na żywo w przeglądarce i niezacommitowane w momencie tego wpisu** — zalecana weryfikacja: kontrast tekstu na nowej palecie (zwłaszcza `--foreground` na `--accent-soft`), czy brak trybu ciemnego jest zamierzony na stałe czy tymczasowy, wygląd stopki na telefonie)
- [x] Poprawki N1–N10 z `15-przeglad-kodu-poprawki.md` (2026-07-17, przegląd kodu 2026-07-17; aplikacja działała poprawnie, to usprawnienia odporności/kosztów/porządki, nie naprawa awarii). **Krytyczne**: N1 budżet kontekstu w `chat/route.ts` — zeskrapowana dokumentacja przycięta z 600k do 350k znaków, odpowiedź z 64k do 32k tokenów, historia rozmowy ucinana od najstarszych wiadomości do 100k znaków (wcześniej szła bez limitu — przy długiej rozmowie i kilku źródłach API zwracało błąd okna 200k). N2 fragment od `getSystemPrompt()` do utworzenia streamu obudowany try/catch — błąd w klasyfikacji/zapisie/budowie zapytania teraz zwraca pytanie do puli zamiast je tracić (dotąd zwrot działał tylko dla błędów samego streamu). N3 dodane indeksy (`Conversation`, `Message`×2, `ScrapedSource`×2, `ScrapedPage`, `Purchase`) — migracja `20260717115835_add_indexes` zawiera wyłącznie `CREATE INDEX`. **Ważne**: N4 `cleanupOldFreeQuota()` w `quota.ts` kasuje klucze `ip:...`/`login:...` starsze niż 7 dni (odpalane bez `await` na końcu `/api/chat`); klucze `device:...` celowo nietykane — to trwała pula na urządzenie. N5 wywołanie `stripe.checkout.sessions.create` w `checkout/route.ts` obudowane try/catch — przy błędzie Stripe zakup oznaczany `failed` zamiast wisieć jako `pending` na zawsze. N6 `/api/me` liczy darmowe pytania jako minimum z trzech pul (konto, urządzenie, IP/dzień) zamiast tylko puli konta — licznik już nie pokazuje więcej pytań niż faktycznie dostępne. **Porządki**: N7 usunięta martwa zmienna `appliesToUs` w `robots.ts` (nie wpływała na wynik). N8 komunikat błędu scrapowania wydzielony do `src/lib/scraper/messages.ts` (`SCRAPE_FAILED_MESSAGE`), używany w `scrape/route.ts` i `ChatApp.tsx` zamiast dwóch osobnych literałów. N9 usunięte pliki-resztki z create-next-app (`next.svg`, `vercel.svg`, `globe.svg`, `file.svg`, `window.svg` z `public/`) po potwierdzeniu grepem braku użycia. **Opcjonalne**: N10 `getAdminStats` w `src/lib/admin/stats.ts` liczy tokeny/koszt agregacją SQL (`prisma.message.groupBy`) zamiast wczytywać wszystkie wiadomości asystenta do pamięci — struktura `AdminStats` bez zmian. N11 (świadomie nietykane pozycje) pominięte zgodnie z instrukcją. `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run` na testach niezależnych od bazy (quota, ssrf, html) — 25/25 zielone (`npm test` nadal pominięty guardem P12 z braku testowej bazy Neon). **Nieprzetestowane na żywo w przeglądarce** — właściciel świadomie pominął ten krok przy tym przeglądzie; zalecana późniejsza weryfikacja: czat z długą historią i zeskrapowaną dokumentacją, licznik pytań w panelu bocznym po `/api/me`, `/admin` (statystyki po zmianie N10), przycisk „Kup pakiet" do przekierowania Stripe.
- [x] Poprawki z `POPRAWKI-PRZEGLAD-KODU-2026-07-17.md` (2026-07-18, przegląd kodu 2026-07-17, wykonane w trzech etapach z osobnymi commitami). **Etap 1** (backend, bez zmian schematu): 1.1 gałąź reuse w `scrape/route.ts` robi teraz `upsert` zamiast `update` — wcześniej usunięta organizacja po ponownym dodaniu tego samego adresu nie wracała na listę „Moje organizacje" (`update` po skasowanym wierszu rzucał P2025, cicho łykany przez `.catch(() => {})`); 1.2 budżet kontekstu AI (`MAX_SCRAPED_CONTEXT_CHARS` = 350k znaków) w `chat/route.ts` dzielony sprawiedliwie między źródła (pętla liczy `sourceBudget` na podstawie liczby pozostałych źródeł, niewykorzystana reszta przechodzi dalej) zamiast oddawać całą pulę pierwszemu źródłu — duża organizacja nie wypycha już treści konkursu z kontekstu; nagłówek źródła też jest teraz przycinany do budżetu; 1.3 reuse rozszerzony na `kind === "grant"`, ale tylko gdy poprzednie pobranie ma mniej niż 24h (`grantReuseCutoff`) — klik w zapisany konkurs nie zużywa już limitu 10 analiz/godzinę przy każdym kliknięciu (**uwaga**: instrukcja w jednym miejscu wspominała „2 tygodnie" a w drugim podawała wzór na 24h i tak też potwierdzała sekcja weryfikacji — zastosowano 24h). **Etap 2** (frontend, `ChatApp.tsx`): 2.1 `refreshSavedSources` sprawdza `r.ok` i `Array.isArray` przed zapisem do stanu — wygasła sesja (401 z obiektem błędu) nie wywala już czatu białym ekranem; 2.2 usuwanie zapisanej organizacji/konkursu scalone w `handleDeleteSaved(kind, id)`, sprawdza wynik żądania i pokazuje komunikat błędu (`savedError`) zamiast cicho znikać przy nieudanym DELETE; 2.3 pole „+ dodaj link” w `SavedSection` ma teraz własny lokalny stan zamiast współdzielić bufor z `SourceForms`; 2.4 dodany przycisk „ⓘ” przy każdej zapisanej pozycji, rozwijający opis pod wierszem (`expandedId`) — opis dostępny też na dotyku, dymek na hover działa jak dotąd. **Etap 3** (refaktoryzacja schematu, za zgodą właściciela): nowy model `SavedSource` (pole `kind`) zastępuje bliźniacze `UserOrganization`/`UserGrant`; migracja `20260718123347_replace_user_organization_grant_with_saved_source` najpierw tworzy nową tabelę i **przenosi dane** (`INSERT ... SELECT`), dopiero potem usuwa stare tabele — zweryfikowane po migracji na bazie Neon (1 wiersz organizacji „Szkoła Liderstwa” przeniesiony poprawnie); jedna trasa `/api/saved-sources` (GET z opcjonalnym `?kind=`, DELETE przez `deleteMany`) zastępuje cztery stare trasy `/api/organizations[/[id]]` i `/api/grants[/[id]]`; `scrape/route.ts` ma teraz jeden `prisma.savedSource.upsert`; `ChatApp.tsx` trzyma jeden stan `savedSources` (filtrowany lokalnie do dwóch sekcji) i wydzielony pomocnik `createConversation()` używany w trzech miejscach, które wcześniej powtarzały tę samą sekwencję zakładania rozmowy. `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npm test` — 25/25 zielone (test webhooka nadal pominięty guardem z braku testowej bazy Neon, jak w poprzednich etapach); zweryfikowano na żywo przez curl, że stare trasy zwracają 404, a nowa `/api/saved-sources` odpowiada poprawnie (401 bez sesji). **Nieprzetestowane w przeglądarce jako zalogowany użytkownik** — zalecana weryfikacja właściciela: dodanie/usunięcie organizacji i konkursu w panelu bocznym, przycisk „ⓘ” na telefonie, rozmowa z organizacją (350k znaków) i konkursem jednocześnie sprawdzająca, czy AI widzi treść obu.
- [x] Etap 9 — wdrożenie na produkcję (2026-07-25, wg `dokumentacja-aplikacja-granty/16-wdrozenie.md`; aplikacja żyje pod `https://asystent.dobryai.pl` na Vercelu (projekt `dobryai/asystent-grantowy`), baza produkcyjna to osobny projekt Neon „Asystent grantowy_prod" (`withered-union-58447667`, eu-central-1); strona odpowiada 200, logowanie mailem przetestowane na żywo bez błędów runtime. **Web Analytics**: włączone w ustawieniach projektu na Vercelu + dodany pakiet `@vercel/analytics` i komponent `<Analytics />` w `src/app/layout.tsx` (bez tego samo włączenie w dashboardzie nie zbierałoby danych); wzmianka o tym dopisana do `content/prawne/polityka-prywatnosci.md` (cel przetwarzania, bez wymogu zgody na cookies — Analytics jest bezcookie'owe); Speed Insights świadomie pominięte na razie. **Naprawiony realny problem infrastrukturalny**: pierwszy production deploy po dłuższej przerwie padał na `npx prisma migrate deploy` błędem `P1002` (timeout advisory locka) — przyczyna: migracje szły przez pooled endpoint Neona (PgBouncer), który nie obsługuje poprawnie session-level advisory locków; naprawione dodaniem zmiennej `DIRECT_URL` (bezpośrednie, non-pooled połączenie) i przestawieniem `prisma.config.ts` na jej użycie dla CLI — runtime aplikacji (`src/lib/db.ts`) nadal łączy się przez pooled `DATABASE_URL`, bez zmian; `DIRECT_URL` dodane dla Production i Preview, udokumentowane w `16-wdrozenie.md` (Krok 3 + nowa sekcja „Gdy coś nie działa"). **Rotacja hasła Neona**: przy debugowaniu connection string do bazy (z hasłem) przeszedł przez czat z asystentem AI — jako środek ostrożności zrotowano hasło roli `neondb_owner`, zaktualizowano `DATABASE_URL` i `DIRECT_URL` na Vercelu (oba środowiska), zweryfikowano że build i logowanie (zapis do bazy) działają z nowym hasłem. **Nadal do zrobienia z instrukcji w `16-wdrozenie.md`**: Krok 5 (aktywacja produkcyjnego Stripe + webhook z czterema zdarzeniami), Krok 6 (weryfikacja domeny nadawczej w Resend), uzupełnienie placeholderów `[UZUPEŁNIĆ: ...]` w `content/prawne/*.md`.)
- [x] Szybkość działania i nieruchome menu (2026-07-27, zgłoszenie właściciela: „produkcja bardzo wolno działa", „menu po lewej przesuwa się wraz z rosnącą rozmową"). **Układ**: okno przeglądarki nie przewija się już wcale — `body` z `min-h-full` na `h-full overflow-hidden`, a przewijanie przeniesione do ramki wewnątrz (`layout.tsx`), dzięki czemu strony tekstowe (`/regulamin`, `/pakiety`, `/admin` — korzystają z `flex-1` bez `min-h-0`) przewijają się jak dotąd, a w czacie przewija się wyłącznie lista wiadomości; dodane `min-h-0`/`flex-shrink-0` w `page.tsx` i `ChatApp.tsx`, żeby nagłówek i panel boczny miały stałą wysokość. **Szybkość** (cztery niezależne przyczyny): (1) `proxy.ts` odpytywał bazę o sesję przy KAŻDYM żądaniu (`auth()` + strategia `database` = 2 zapytania do Neona na każdą stronę i każde `/api/*`); teraz sprawdza wyłącznie OBECNOŚĆ ciasteczka sesji (`authjs.session-token` / `__Secure-authjs.session-token`) — to tylko wygoda (przekierowanie na logowanie), prawdziwa weryfikacja tożsamości pozostaje bez zmian w każdej stronie i każdym `/api` przez `auth()`; `page.tsx` robi teraz `redirect("/logowanie")` zamiast `return null`, żeby wygasłe ciasteczko nie kończyło się białym ekranem; zmierzone: pośrednik z setek ms do ~1,5 ms. (2) `ChatApp.tsx` przerysowywał całą rozmowę (z ponownym parsowaniem Markdownu wszystkich wiadomości) przy każdym kawałku strumienia — dodany memoizowany komponent `Markdown` i stała `REMARK_PLUGINS`, oraz throttling zapisu do stanu do 80 ms (`flush`), co ograniczyło liczbę renderów ze setek na sekundę do ~12. (3) `scrollIntoView({behavior:"smooth"})` odpalany przy każdej literce — zastąpiony przewijaniem kontenera (`scrollAreaRef`) z płynną animacją tylko przy nowej wiadomości/źródle i natychmiastowym w trakcie strumienia, plus `stickToBottomRef` (przewinięcie w górę wstrzymuje autoscroll). (4) `settings.ts` — prompt systemowy i limit darmowych pytań trzymane 60 s w pamięci procesu (zapis w panelu admina czyści wpis natychmiast, ale na innych instancjach serverless zmiana może zadziałać do minuty później); `chat/route.ts` odpowiada nagłówkami `Cache-Control: no-cache, no-store, no-transform` i `X-Accel-Buffering: no`, żeby pośrednicy nie buforowali strumienia. **Rozdzielenie komunikatów błędów AI** (zgłoszone przy okazji — nieważny klucz API pokazywał się jako „Chwilowe przeciążenie, spróbuj za minutę" i wyglądał na awarię przejściową): nowe `isAiConfigError()` i `AI_CONFIG_ERROR_MESSAGE` w `lib/ai/client.ts` rozpoznają HTTP 400/401/403/404 (zły/wygasły klucz, brak uprawnień, zła nazwa modelu) i pokazują osobny komunikat bez technicznych szczegółów, a w logu serwera zostawiają `[AI — BŁĄD USTAWIEŃ, HTTP xxx]`; użyte w `chat/route.ts` (oba miejsca: przygotowanie zapytania i błąd streamu) i w `scrape/route.ts` (wcześniej awaria podsumowania AI wypisywała mylące „Wystąpił błąd podczas pobierania strony", choć pobranie się udało); przy okazji klient Anthropic w trybie dev nie jest już współdzielony między przeładowaniami, gdy zmienił się klucz — po podmianie w `.env.local` serwer uparcie używał starego. Nowy test `src/lib/ai/client.test.ts` (3 przypadki). `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; testy niezależne od bazy 103/103 zielone (test webhooka nadal pominięty guardem z braku testowej bazy Neon). Zweryfikowane lokalnie: przekierowanie `/` → `/logowanie` bez pętli, czasy pośrednika w logu dev, nowy klucz API (HTTP 200 na `api.anthropic.com`). **Nieprzetestowane w przeglądarce jako zalogowany użytkownik** (logowanie przez magic link) — zalecana weryfikacja właściciela: czy menu boczne, nagłówek i pole wpisywania stoją w miejscu przy długiej rozmowie, czy odpowiedź pisze się płynnie w rozmowie z kilkunastoma wiadomościami, czy przewinięcie w górę w trakcie odpowiedzi nie ściąga widoku na dół.
- [x] Czekanie przed pierwszym słowem odpowiedzi — wariant A (2026-07-27, zgłoszenie: „przy każdym pytaniu serwer wczytuje z bazy całą treść zeskrapowanych stron (nawet kilka MB)"). Przyczyna potwierdzona w kodzie: `chat/route.ts` pobierał przy każdym pytaniu wszystkie `ScrapedPage.textContent` (limity scrapera to 15–20 stron HTML + 10 PDF-ów **na źródło**, PDF do 8 MB, tekst zapisywany bez przycięcia), a używał z tego najwyżej `MAX_SCRAPED_CONTEXT_CHARS` = 350 tys. znaków — reszta transferu z Neona szła do kosza. **Zmiany**: (1) nowe pole `ScrapedSource.contextBlob` (migracja `20260727135815_add_scraped_source_context_blob`, zastosowana na lokalnej bazie Neon) — gotowy, przycięty do 350k znaków kawałek promptu, składany RAZ po scrapowaniu w `scrape/route.ts` (obie ścieżki: świeży crawl i kopiowanie poprzedniego pobrania); logika składania i sprawiedliwego dzielenia budżetu między źródła wyniesiona z trasy czatu do nowego `src/lib/ai/context.ts` (`buildSourceContext`, `assembleScrapedContext`) — zachowanie identyczne co do formatu promptu, nowy test `src/lib/ai/context.test.ts` (6 przypadków). (2) Zapytanie czatu nie pobiera już `pages` — czyta jeden wiersz na źródło; źródła sprzed migracji (`contextBlob` puste) mają blob składany w locie ze stron i **uzupełniany w tle**, więc drugie pytanie w takiej rozmowie jest już szybkie. (3) `cache_control` na dokumentacji dostał `ttl: "1h"` zamiast domyślnych 5 minut — dłuższa przerwa między pytaniami nie oznacza już przetwarzania ~100 tys. tokenów od zera. (4) Zapis pytania użytkownika i ustawienie tytułu rozmowy lecą równolegle z budowaniem zapytania do AI (`await writes` dopiero po wystartowaniu strumienia), zamiast blokować je sekwencyjnie. (5) **Pomiary**: przy pierwszym słowie odpowiedzi w logu serwera ląduje linia `[czat] baza X ms, kontekst Y ms (Z znaków), pierwsze słowo po W ms, model M` — na tej podstawie decydujemy, czy potrzebny jest wariant B. `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; testy niezależne od bazy 109/109 zielone (test webhooka nadal pominięty guardem z braku testowej bazy Neon). **UWAGA WDROŻENIOWA**: na produkcji trzeba uruchomić `npx prisma migrate deploy` na produkcyjnej bazie — Vercel nie robi migracji sam (`build` to samo `next build`). **Nieprzetestowane w przeglądarce jako zalogowany użytkownik** — zalecana weryfikacja właściciela: zadanie pytania w rozmowie ze starą, już zeskrapowaną dokumentacją (pierwsze pytanie może być wolne jak dotąd, kolejne wyraźnie szybsze), zeskrapowanie nowej strony i pytanie do niej, oraz odczyt czasów z logów Vercela. **Nierobione świadomie — wariant B** (podsumowania + spis stron w prompcie zamiast całej dokumentacji, plus narzędzie `przeczytaj_strone` czytające stronę z bazy na żądanie): właściwa zmiana sposobu podawania dokumentacji do AI, do decyzji po zobaczeniu pomiarów.
- [ ] Koszty AI i czas odpowiedzi (instrukcja: `dokumentacja-aplikacja-granty/17-koszty-i-latencja.md`, spisana 2026-07-27). Powód: przed publicznym startem, na 3 pytaniach testowych, koszt AI wyniósł **$1,11 ≈ 1,45 zł za pytanie**, przy cenie sprzedaży 0,33–0,50 zł — pytanie kosztuje 3–4× więcej, niż użytkownik za nie płaci. Po wariancie A odczyt bazy przestał być wąskim gardłem (ok. 1 s z ~9 s do pierwszego słowa); zostały dwa źródła kosztu i czekania: ~87 tys. tokenów dokumentacji wysyłane przy każdym pytaniu (zapis do cache 1,3–2,1 zł) oraz tryb rozumowania włączony dla każdego pytania z dokumentacją (tokeny wyjściowe po $15/MTok, w streamie widoczne jako cisza przed odpowiedzią). Etap 1 (godziny): cofnięcie `ttl: "1h"` na 5 minut (godzinny cache kosztuje 2× zamiast 1,25× i opłaca się tylko przy długich przerwach — zmiana z 2026-07-27 była pomyłką kosztową), rozumowanie i `max_tokens` zależne od charakteru pytania (heurystyka bez dodatkowego wywołania AI; model dalej Sonnet — zasada 5 z CLAUDE.md), wskaźnik „Analizuję dokumentację…" zamiast pustki przy rozumowaniu, log `[czat]` rozszerzony o cache i czas do pierwszego zdarzenia. Etap 2 (2–3 dni): wariant B — w prompcie zostają podsumowania i spis stron (~3 tys. tokenów), model dociąga treść narzędziami `przeczytaj_strone`/`szukaj_w_dokumentacji`; z przełącznikiem `ai_docs_mode` w panelu admina pozwalającym wrócić do starego trybu jednym kliknięciem. Etap 3 (po Etapie 2): pytania wyszukujące na Haiku (3× taniej), Sonnet zostaje przy analizie kwalifikowalności i pisaniu wniosku — wcześniej się nie opłaca, bo cache jest osobny dla każdego modelu i mieszanie ich przy 87 tys. tokenów w prompcie mnoży zapisy do cache; wymaga też zmiany zasady 5 w CLAUDE.md. Do decyzji właściciela (nie agenta): czy rozumowanie wyłączyć całkiem — instrukcja każe pokazać do porównania dwie wersje odpowiedzi na to samo pytanie o uzasadnienie projektu, z kosztem i czasem. Cel: koszt pytania poniżej 0,30 zł, pierwsze słowo poniżej 3 s dla pytań prostych. **Stan na 2026-07-28: Etap 0 i Etap 1 zrobione, rozumowanie wyłączone na stałe (decyzja właściciela) — szczegóły w dwóch wpisach niżej. Etap 2 i Etap 3 nierozpoczęte.**
- [x] Koszty AI — Etap 1 (2026-07-27, instrukcja `dokumentacja-aplikacja-granty/17-koszty-i-latencja.md`). Zrobione: (1) `ttl: "1h"` w `cache_control` w `src/app/api/chat/route.ts` cofnięte na domyślne 5 minut — zapis do cache kosztuje znów 1,25× zamiast 2× ceny wejścia; w kodzie i w `src/lib/admin/stats.ts` zostały komentarze, że powrót do `ttl: "1h"` wymaga jednoczesnej zmiany `CACHE_WRITE_MULTIPLIER` na 2.0. (2) Rozumowanie i `max_tokens` zależne od charakteru pytania: nowa funkcja `needsDeepThinking()` w `src/lib/ai/router.ts` (heurystyka tekstowa, bez dodatkowego wywołania AI) — pytania wytwórcze („napisz", „przygotuj", „harmonogram", „budżet"…) i wiadomości dłuższe niż 300 znaków dostają `thinking: adaptive` i `max_tokens: 32000`, pytania faktograficzne — bez rozumowania i `max_tokens: 4096`; testy w `src/lib/ai/router.test.ts` (4 przypadki). Model bez zmian — dalej Sonnet dla rozmów z dokumentacją (zasada 5 z CLAUDE.md). (3) Wskaźnik pracy modelu: serwer przy pierwszym `thinking_delta` wysyła znacznik statusu (`src/lib/chat-stream.ts`, wstawka otoczona znakiem U+001F — bez przebudowy strumienia na NDJSON), `ChatApp.tsx` wycina go z tekstu i pokazuje „Analizuję dokumentację…" przy animowanych kropkach, do pierwszego słowa odpowiedzi; treść rozumowania nie jest wysyłana do przeglądarki. (4) Log `[czat]` rozszerzony o tokeny cache (zapis/odczyt, z `message_start`), czas do pierwszego zdarzenia obok czasu do pierwszego słowa oraz informację, czy rozumowanie było włączone. Weryfikacja: `npx tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run src/lib` — 113 testów zielonych, czerwony tylko znany guard `webhook.test.ts` (wymaga bazy testowej). **Nieprzetestowane na żywo** — brak pomiaru bazowego (Etap 0) i pomiarów po zmianie; do wykonania przez właściciela na trzech pytaniach testowych P1/P2/P3. **Do decyzji właściciela**: porównanie odpowiedzi na P3 z rozumowaniem i bez (jakość vs koszt i czas) — jeśli różnicy nie widać, `needsDeepThinking()` ma zwracać zawsze `false`. **Nierozpoczęte**: Etap 2 (wariant B — dokumentacja na żądanie) i Etap 3 (Haiku dla pytań wyszukujących).
- [x] Rozumowanie wyłączone na stałe (2026-07-28, decyzja właściciela po porównaniu odpowiedzi na to samo pytanie o uzasadnienie projektu z rozumowaniem i bez — różnicy w jakości nie było, a rozumowanie kosztowało ~6 s czekania i tokeny wyjściowe po $15/MTok). Stała `THINKING_ENABLED = false` w `src/lib/ai/router.ts`; heurystyka została, ale pod własną nazwą `looksLikeWritingTask()` — steruje teraz wyłącznie limitem długości odpowiedzi (`max_tokens`: 32k dla pytań wytwórczych, 4096 dla faktograficznych), żeby wyłączenie rozumowania nie ucinało długich wniosków w pół zdania. Powrót = zmiana stałej na `true`. Zmienna `AI_THINKING` (`on`/`off`) nadal wymusza tryb do porównań — na produkcji nieustawiona. Testy: `src/lib/ai/router.test.ts` (10 przypadków). Pomiary z 2026-07-27, które doprowadziły do decyzji: dokumentacja w prompcie ma **153 391 tokenów** (350 004 znaki, czyli górny limit `MAX_SCRAPED_CONTEXT_CHARS` — dokumentacja jest już przycinana), a nie ~87 tys. jak zakładała instrukcja 17; zapis do cache przy pierwszym pytaniu w rozmowie to ~$0,58 wg cen panelu (~2,30 zł), kolejne pytania z trafionym cache'em ~$0,05. Pierwsze słowo przy pytaniu prostym bez rozumowania: 7,2 s, z czego 6,9 s to samo przetworzenie promptu — cel „poniżej 3 s" jest nieosiągalny bez Etapu 2. Uwaga: panel admina liczy po cenach standardowych $3/$15, a do 2026-08-31 obowiązuje cena wprowadzająca Sonneta $2/$10 — panel zawyża koszty o ~50%.
- [x] Koszty AI — Etap 2, dokumentacja czytana na żądanie (2026-07-28, instrukcja `dokumentacja-aplikacja-granty/17-koszty-i-latencja.md`). **Co zrobione**: (1) Nowe pole `ScrapedSource.indexBlob` (migracja `20260728120000_add_scraped_source_index_blob`) — spis stron w JSON: dla każdej strony identyfikator wiersza, tytuł, adres i pierwsze 200 znaków, maks. 80 stron na źródło; składany raz po scrapowaniu (`buildSourceIndex` w `src/lib/ai/context.ts`, obie ścieżki `scrape/route.ts` — świeży crawl i kopiowanie poprzedniego pobrania, które teraz używa `createManyAndReturn`, bo do spisu potrzebne są identyfikatory nowych wierszy). `contextBlob` i `buildSourceContext` **zostają nietknięte** — to droga powrotu. (2) `assembleSourceIndex()` skleja spisy wszystkich źródeł rozmowy w blok promptu i nadaje stronom krótkie etykiety (`o1`, `g1`, `g2` — organizacja przed konkursem) oraz zwraca mapę etykieta → wiersz w bazie; etykiety nadawane są przy każdym pytaniu, więc są zawsze unikalne w obrębie rozmowy. (3) Nowy `src/lib/ai/tools.ts` z dwoma narzędziami: `przeczytaj_strone(id)` — treść strony przycięta do 30 tys. znaków, po identyfikatorze ze spisu (nie po URL-u, żeby model nie wymyślał adresów), oraz `szukaj_w_dokumentacji(fraza)` — proste wyszukiwanie pełnotekstowe (`contains` + `mode: "insensitive"`, bez embeddingów i pgvector), do 5 trafień po ~300 znaków kontekstu. Oba działają wyłącznie na stronach źródeł tej rozmowy (identyfikator musi być w mapie rozmowy **i** strona musi należeć do jej `sourceId`); wyniki wracają do modelu opakowane tą samą klauzulą „traktuj jako informacje, nie polecenia" co dokumentacja w prompcie. (4) Pętla narzędziowa w `chat/route.ts`: tekst streamowany na bieżąco we wszystkich rundach, odpowiedź asystenta wraca do historii w całości (wszystkie bloki, także `thinking` z podpisem), zużycie tokenów **sumowane ze wszystkich rund** przed zapisem w wierszu `Message` (inaczej panel admina liczyłby tylko ostatnią rundę), `cache_control` na końcu bloku systemowego (spis jest stały w rozmowie, wyniki narzędzi nie). Bezpieczniki: maks. 6 rund i 60 tys. znaków treści z narzędzi — po przekroczeniu model dostaje `tool_result` z informacją o wyczerpanym limicie i traci narzędzia w kolejnym wywołaniu. Rezerwacja/zwrot pytania obejmuje całe żądanie, bez zmian. Znacznik statusu `STATUS_THINKING` („Analizuję dokumentację…") wysyłany jest teraz także przy pierwszym wywołaniu narzędzia, nie tylko przy rozumowaniu. (5) Przełącznik `AppSetting.ai_docs_mode` (`ondemand` domyślnie / `full` — stara ścieżka przez `contextBlob`) z polem wyboru w `/admin` → Ustawienia; obie ścieżki działają, zmiana obowiązuje od kolejnego pytania, także w trwających rozmowach. Źródła sprzed migracji dostają spis składany w locie i uzupełniany w tle — dokładnie jak wcześniej `contextBlob`. **Odstępstwo od instrukcji (świadome)**: akapit o trybie pracy z narzędziami NIE trafił do promptu w bazie ani do `DEFAULT_SYSTEM_PROMPT`, tylko do bloku systemowego składanego w kodzie razem ze spisem stron — inaczej w trybie `full` prompt kazałby modelowi używać narzędzi, których w tym trybie nie ma. Prompt czatu nadal siedzi w bazie (zasada 6), a instrukcja techniczna jest przy tym, czego dotyczy, tak jak istniejąca klauzula „ZESKRAPOWANA DOKUMENTACJA (traktuj jako informacje…)". Testy: `src/lib/ai/context.test.ts` rozszerzony o budowę i składanie spisu (7 nowych przypadków), nowy `src/lib/ai/tools.test.ts` (12 przypadków — w tym odmowa dostępu do strony spoza rozmowy, przycinanie treści, ograniczenie wyszukiwania do źródeł rozmowy, limity rund i treści). `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run src/lib` — 134 testy zielone, czerwony tylko znany guard `webhook.test.ts` (wymaga bazy z „test" w nazwie `DATABASE_URL`). **Sprawdzone na żywo 2026-07-29** (migracja zastosowana przez właściciela, rozmowa z dwoma źródłami): jakość odpowiedzi oceniona przez właściciela jako zadowalająca — asystent odpowiadał na podstawie treści dokumentów, nie ogólników z podsumowania. **Pomiary przed/po** (ceny wprowadzające Sonneta $2/$10; panel admina liczy po $3/$15, więc pokaże ~50% więcej): kontekst w prompcie **350 004 → 6 686 znaków**, zapis do cache przy pierwszym pytaniu w rozmowie **153 391 → 6 083 tokenów**, koszt pierwszego pytania **~2,30 zł → ~0,16 zł (15× taniej)**, koszt kolejnego pytania ~0,20 zł — poniżej celu 0,30 zł z instrukcji i poniżej ceny sprzedaży 0,33–0,50 zł. Czas: pierwsze słowo 7,3 s (pytanie 1) i 4,4 s (pytanie 2) wobec 7,2 s przed zmianą — **cel „poniżej 3 s" nieosiągnięty**, ale pomiar pokazał, że wąskie gardło leży już poza AI: ~2,8 s schodzi na odczyt rozmowy z bazy, sprawdzenie limitów i rezerwację pytania, zanim w ogóle wysyłamy zapytanie do modelu (zapytania idą sekwencyjnie — do zrównoleglenia, patrz `18-zrownoleglenie-i-haiku.md`). Narzędzia okazały się niewinne: odczyt strony z Neona to 117–233 ms, wyniki narzędzi to ~17 tys. znaków na pytanie, przygotowanie promptu w rundzie po narzędziach 1,1–2,9 s. Zaobserwowane przy okazji: model często pisze zdanie **zanim** sięgnie po dokumenty, dzięki czemu pierwsze słowo pada wcześniej niż gotowa odpowiedź. **Jedna liczba niewyjaśniona**: w jednym przebiegu runda odpowiedzi miała pierwsze zdarzenie po 1 085 ms, a pierwsze słowo dopiero po 16 274 ms (przy wyłączonym rozumowaniu); we wcześniejszym przebiegu zdarzyło się jednorazowe 55 s na całe pytanie, które się nie powtórzyło — podejrzenie pada na chwilową zadyszkę API, nie potwierdzone. **Rozszerzony pomiar w logu** (dopisany 2026-07-29): linie `[czat/runda N]` (czas odpowiedzi modelu w rundzie + czas i rozmiar wyniku każdego narzędzia), `[czat/runda N — odpowiedź]` (przygotowanie promptu, pierwsze słowo, tokeny, `powód zakończenia` — po nim widać ucięcie odpowiedzi na `max_tokens`) i `[czat/razem]` (suma tokenów ze wszystkich rund, czyli prawdziwy koszt pytania). Uwaga na przyszłość: pytanie „czy organizacja spełnia kryteria merytoryczne" nie zawiera żadnego ze słów z `WRITING_HINTS`, więc dostaje limit 4096 tokenów zamiast 32 000 — tym razem odpowiedź się zmieściła (`end_turn`), ale przy dłuższej analizie kwalifikowalności może zostać ucięta.
- [ ] Zrównoleglenie zapytań do bazy + Haiku dla pytań wyszukujących (instrukcja: `dokumentacja-aplikacja-granty/18-zrownoleglenie-i-haiku.md`, spisana 2026-07-29 po pomiarach Etapu 2). Etap A: ~2,8 s przed pierwszym wywołaniem AI schodzi na sekwencyjne zapytania do Neona — do zrównoleglenia. Etap B: po Etapie 2 w prompcie zostało ~6 tys. tokenów zamiast 153 tys., więc argument „jeden cache zamiast dwóch" przestał obowiązywać i pytania wyszukujące mogą iść na Haiku (3× taniej). Wymaga zmiany zasady 5 w `CLAUDE.md` i decyzji w `dokumentacja-aplikacja-granty/README.md`. **Stan na 2026-07-29: Etap A i Etap B zrobione w kodzie, pomiary na żywo do wykonania przez właściciela — szczegóły we wpisach niżej.**
- [x] Zrównoleglenie — Etap A (2026-07-29, instrukcja `dokumentacja-aplikacja-granty/18-zrownoleglenie-i-haiku.md`). Odczyt rozmowy, licznik rate limitu, `getFreeQuestionsLimit`, `getSystemPrompt`, `getAiDocsMode` i ciasteczko `ag_device` szły w `src/app/api/chat/route.ts` jedno po drugim — każde to osobna podróż do Neona. Teraz lecą równolegle w jednym `Promise.all` (zapytanie o rozmowę wyniesione do pomocnika `loadConversation`). **Kolejność rezerwacji bez zmian** (zasada 2): sprawdzenie właściciela rozmowy → rate limit → `reserveQuestion` → budowa promptu → `anthropic.messages.stream`. Odczyt ustawień przeniesiony przed rezerwację, więc błąd bazy przy ustawieniach daje 500 z czytelnym komunikatem i **nie zabiera pytania z puli** (wcześniej leciał po rezerwacji i wymagał zwrotu). W logu `[czat]` doszła pozycja `przygotowanie N ms` — czas od startu żądania do wysłania pierwszego zapytania do modelu (wcześniej trzeba go było liczyć z różnicy). `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run src/lib` — 134 testy zielone, czerwony tylko znany guard `webhook.test.ts`. **Nieprzetestowane na żywo** — do wykonania przez właściciela: pytanie w cudzej rozmowie (404 i licznik bez zmian), wyczerpana pula, piąte pytanie w minucie, normalne pytanie; w logu `przygotowanie` powinno spaść z ~2,8 s do ~1–1,5 s.
- [x] Haiku dla pytań wyszukujących — Etap B (2026-07-29, ta sama instrukcja). Nowa heurystyka `looksLikeLookupQuestion` i `pickDocsModelClass` w `src/lib/ai/router.ts`: w rozmowie z dokumentacją pytanie **wyszukujące** (termin, kwota, załączniki, „kto może składać wniosek") idzie na Haiku, a **analiza kwalifikowalności i pisanie treści wniosku zostają na Sonnecie**. **Świadomie bez klasyfikatora AI** (`classifyQuestion` dokłada 0,3–0,6 s i własny koszt do KAŻDEGO pytania, także tych idących na Sonneta — sprzeczne z celem „pierwsze słowo poniżej 3 s"); decyzja właściciela z 2026-07-29. Heurystyka jest zachowawcza: wymaga trafienia w listę zwrotów wyszukujących, odrzuca pytania dłuższe niż 200 znaków, ze słowami analitycznymi („kwalifikowal", „oceń", „szanse", „doradź", „ryzyk"…) i z czasownikami wytwórczymi („napisz", „przygotuj"…) — przy wątpliwości wybiera Sonneta. Osobna, węższa lista czasowników niż `WRITING_HINTS`, bo tamta zawiera rzeczowniki („wniosek", „plan", „budżet") padające w zwykłych pytaniach o fakty. **Mieszanie modeli tylko w trybie `ai_docs_mode = "ondemand"`** — w trybie `full` dalej zawsze Sonnet (cache osobny dla każdego modelu, przy 150+ tys. tokenów drugi zapis kasuje oszczędność). Zmieniona **zasada 5 w `CLAUDE.md`** i decyzja o routerze w `dokumentacja-aplikacja-granty/README.md`. Testy: `src/lib/ai/router.test.ts` rozszerzony o 9 przypadków (pytania wyszukujące → Haiku, kwalifikowalność i pisanie → Sonnet, tryb `full` → zawsze Sonnet, długie pytanie nigdy nie jest wyszukujące, brak dopasowania → Sonnet). `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run src/lib` — 143 testy zielone, czerwony tylko znany guard `webhook.test.ts`. **Nieprzetestowane na żywo — dwie rzeczy do sprawdzenia przez właściciela**: (1) czy Haiku poprawnie używa narzędzi `przeczytaj_strone`/`szukaj_w_dokumentacji`, czy nie zaczyna zgadywać identyfikatorów stron; (2) czy odpowiedzi na P1 są nadal konkretne i zgodne z dokumentami. Jeśli Haiku zgaduje terminy albo kwoty — wrócić z tą kategorią pytań na Sonneta (zmiana w `pickDocsModelClass`). Oczekiwany koszt P1: ~0,16 zł → ~0,05 zł.
- [x] Cache na wynikach narzędzi, wskaźnik pracy i poprawka routera (2026-07-29 wieczór, backlog dalszych prac: `dokumentacja-aplikacja-granty/19-backlog-optymalizacji.md`). **Co zrobione**: (1) Migracja `20260728120000_add_scraped_source_index_blob` zastosowana na produkcyjnej bazie Neon — wcześniej brak kolumny `indexBlob` wywracał `/api/conversations/[id]` na localhoście (serwer zwracał HTML zamiast JSON, przeglądarka rzucała `SyntaxError`); w `.env.local` dopisane `DIRECT_URL` wymagane przez `prisma.config.ts`. (2) `src/lib/ai/cache.ts` — `markToolResultsForCache` przesuwa punkt cache'owania na koniec ostatniej rundy narzędziowej, bo wcześniej każda kolejna runda wysyłała całą narosłą historię jako świeże wejście (w logu produkcyjnym 94 tys. tokenów wejścia przy 21 tys. z cache). Znacznik zostaje zawsze jeden — limit API to 4 na zapytanie, jeden zajmuje blok systemowy; pilnuje tego `src/lib/ai/cache.test.ts` (4 przypadki). **Efekt zmierzony**: wejście w rundzie pisania odpowiedzi 43 606 → **2 tokeny**. (3) `STATUS_TOOLS`/`STATUS_WRITING` w `src/lib/chat-stream.ts` — wskaźnik „Czytam dokumentację konkursu…" zapala się przy KAŻDEJ rundzie narzędzi i gaśnie dopiero przy pierwszym słowie odpowiedzi; wcześniej `STATUS_THINKING` leciał raz i gasł po pierwszym zdaniu („Sprawdzę…"), przez co przez kilkadziesiąt sekund czytania stron ekran wyglądał na zawieszony. Potwierdzone na żywo przez właściciela. (4) Poprawka routera: pytanie „biorąc pod uwagę doświadczenie organizacji, wymogi merytoryczne konkursu i terminy czy 3 osobowy zespol poradzi sobie z aplikacja?" (130 znaków) szło na **Haiku**, bo trafiało w `LOOKUP_HINTS` słowem „termin" (wymienionym jako jedna z przesłanek), a w `ANALYSIS_HINTS` nie trafiało w nic — lista rozpoznawała pytania o konkurs, ale nie pytania **o nas**. Dopisane: „poradzi sobie", „poradzimy", „damy radę", „podołamy", „zdążymy", „czy zespół", „wystarczy nam", „czy warto", „biorąc pod uwagę" (z wariantami bez polskich znaków). Realne pytanie dopisane jako przypadek testowy. Potwierdzone na żywo: to samo pytanie idzie teraz na `claude-sonnet-5`, odpowiedź oceniona przez właściciela jako lepsza. (5) `ChatApp.tsx`: wczytywanie rozmowy przez `fetchConversation`/`applyConversation` — sprawdza odpowiedź serwera i pokazuje komunikat po polsku zamiast nieobsłużonego wyjątku w konsoli. Testy: 148 zielonych, czerwony tylko znany guard `webhook.test.ts`; `tsc --noEmit` i `eslint` bez błędów. **Pomiary produkcyjne** (trzy pytania, tryb `ondemand`): 16:11 Sonnet/3 rundy — 73,4 s, cache odczyt 21 117; 16:37 Haiku/1 runda — 22,5 s; 17:17 Sonnet/1 runda po poprawkach — 62,2 s, cache zapis 31 064. **Uwaga metodologiczna**: żadna z tych par nie różni się tylko jedną zmienną, więc efekt samego cache'owania na CZAS nie został jeszcze zmierzony — przy jednej rundzie cache nie ma czego odczytać (zapisuje na przyszłość, która nie nadchodzi) i dokłada ~25% do kosztu zapisu, ok. 6 gr na pytanie. Zadanie 1 z backlogu: zmierzyć na pytaniu wielorundowym. Pozostałe pozycje czasowe w pomiarze 17:17: **29,6 s** czekania na pierwsze słowo po rundzie narzędzi (przy przygotowaniu promptu 892 ms, rozumowanie wyłączone — przyczyna niewyjaśniona) i ~24 s pisania 4047 tokenów odpowiedzi.
- [x] Zadanie 1 z backlogu: zmierzony efekt cache'owania wyników narzędzi (2026-07-29 ok. 21:00, commit `6186dd2`). **Jak**: nowy skrypt `scripts/pomiar-cache.pomiar.ts` (`npm run pomiar:cache`, osobna konfiguracja `vitest.pomiar.config.ts`, żeby NIE uruchamiał się przy `npm test` — wywołuje płatne API). Skrypt zadaje to samo pytanie dwa razy na tym samym modelu i tej samej dokumentacji, różniąc przebiegi wyłącznie wywołaniem `markToolResultsForCache`; wcześniej robi „rozgrzewkę", żeby oba startowały z blokiem systemowym już w cache. Czyta bazę i woła API, nic nie zapisuje (żadnych wiadomości, żadnego zużycia pytań). Pętla narzędziowa jest przepisana z `route.ts` — po zmianie pętli w trasie trzeba poprawić też skrypt. **Wynik przy 4 rundach (Sonnet)**: czas całkowity 50,7 s → 52,5 s (bez zmian), pierwsze słowo w rundzie odpowiedzi 2 329 → 1 835 ms, tokeny wejścia pełną ceną 68 310 → **311**, odczyt z cache 30 415 → 77 985, zapis 0 → 23 270. Koszt wejścia ok. 0,21 → 0,11 USD (**−48%**). Odczyt z cache rósł z rundy na rundę (6 083 → 20 580 → 21 950 → 23 289), czyli mechanizm działa zgodnie z projektem. **Wniosek 1**: cache oszczędza pieniądze, a NIE czas — przygotowanie promptu trwało ok. 2 s w obu wariantach, więc nie było czego skracać; wcześniejsze przypuszczenie, że przyspieszy trudne pytania, było błędne. **Wniosek 2**: realne pytania są jednorundowe — trzy pytania na produkcyjnej dokumentacji (w tym złożone, dwuczłonowe, z jawnymi krokami) dały po jednej rundzie; cztery rundy udało się wywołać dopiero instrukcją „czytaj jedną stronę na raz". To przechyla zadanie 2 z backlogu ku wariantowi „oznaczaj cache dopiero od drugiej rundy", ale przed decyzją potrzebny jest rozkład liczby rund z produkcji. **Przeszkoda**: logi Vercela w tym planie znikają po godzinie, więc rozkładu nie da się wyciągnąć wstecz — trzeba czytać na bieżąco albo dopisać liczbę rund do tabeli `Message`. **Nowy trop do zadania 3**: lokalnie pierwsze słowo padało po 1,1–2,8 s w każdej rundzie, przy 29,6 s w logu produkcyjnym — ale skrypt mierzy czas do pierwszego bloku treści, a `roundFirstTextMs` do pierwszego fragmentu tekstu, więc to trop, nie wniosek. Testy: 148 zielonych, czerwony tylko znany guard `webhook.test.ts`; `tsc --noEmit` bez błędów. **Uwaga operacyjna**: w trakcie pomiarów skończyły się środki na koncie Anthropica (API zwraca „Your credit balance is too low") — to ten sam klucz, którego używa produkcja, więc aplikacja nie odpowiada do czasu zasilenia konta.
- [x] Pomiary w bazie pod zadanie 2 z backlogu + porządek w testach (2026-07-30, backlog: `dokumentacja-aplikacja-granty/19-backlog-optymalizacji.md`). **Problem**: decyzja z zadania 2 (czy oznaczać wyniki narzędzi do cache'u już od pierwszej rundy) wymaga rozkładu liczby rund z prawdziwego ruchu, a logi Vercela w obecnym planie znikają po godzinie — 29.07 nie było już czego czytać z popołudnia. **Zmiana**: tabela `Message` dostała trzy nowe, opcjonalne pola — `toolRounds` (ile razy model sięgnął po dokumentację), `firstTextMs` (czas do pierwszego słowa) i `totalMs` (czas całej odpowiedzi); migracja `20260730113302_add_message_timing_and_tool_rounds` (tylko `ADD COLUMN`, wszystkie `NULL`-owalne — istniejące wiersze nietknięte). Wypełnia je `src/app/api/chat/route.ts` przy zapisie ukończonej odpowiedzi (przerwane odpowiedzi celowo bez pomiarów — zafałszowałyby rozkład). **Panel `/admin`**: nowa ramka „Ile razy AI sięgało do dokumentacji" (rozkład rund z 30 dni, liczby i procenty) i kafelek „Czas odpowiedzi (mediana)" z medianą czasu całkowitego i czasu do pierwszego słowa; w podglądzie rozmowy przy każdej odpowiedzi asystenta widać liczbę rund i czas. Mediany, nie średnie — jedna bardzo długa odpowiedź nie zaburza obrazu. Statystyka pomija wiersze sprzed migracji (pola puste), a licznik „odpowiedzi z pomiarem" mówi, na ilu wierszach jest liczona. **Drobiazg z backlogu**: `vitest.config.ts` wyklucza `.claude/worktrees/**` — vitest uruchamiał testy z trzech roboczych kopii projektu naraz i raportował 148 testów w 21 plikach zamiast 73 w 9. Katalogi worktree zostawione nietknięte (do decyzji właściciela). **Weryfikacja**: `tsc --noEmit`, `npm run lint`, `npm run build` bez błędów; `npx vitest run` — 73 zielone, czerwony tylko znany guard `webhook.test.ts` (wymaga bazy z „test" w nazwie). **Migracja produkcyjna idzie automatycznie**: komenda builda w ustawieniach projektu na Vercelu to `npx prisma migrate deploy && next build` (sprawdzone 30.07 przez `vercel project inspect`) — wcześniejsze wpisy w tym pliku mówiące, że migrację trzeba puszczać ręcznie, są **nieaktualne**. Skutek uboczny do zapamiętania: nieudana migracja wywraca cały deploy. **Nieprzetestowane w przeglądarce** — do sprawdzenia przez właściciela po wdrożeniu: zadać pytanie, potem wejść na `/admin` i zobaczyć, czy w ramce rund przybył wiersz, a w podglądzie rozmowy przy odpowiedzi widać liczbę rund i czas. **Nierobione świadomie**: sama decyzja z zadania 2 — bez danych byłaby zgadywaniem; wracamy do niej, gdy uzbiera się kilkadziesiąt zmierzonych odpowiedzi.
- [x] Jedna rozmowa zamiast dwóch, limit analiz liczony uczciwie, favicon (2026-07-30). **Problem 1 — dwie rozmowy w menu**: po „Nowa rozmowa" automatycznie wczytywała się zapamiętana organizacja (zakładając rozmowę w bazie), a klik w zapisany konkurs zawsze zakładał kolejną — w menu zostawała pusta „Nowa rozmowa" z organizacją obok właściwej rozmowy. **Zmiana**: `handleSelectSaved` w `ChatApp.tsx` dokłada źródło do bieżącej rozmowy, dopóki nie padło w niej ani jedno pytanie i nie ma jeszcze źródła tego samego rodzaju; nową rozmowę zaczyna dopiero przy podmianie konkursu na inny albo gdy rozmowa ma już wiadomości. **Problem 2 — tytuł**: tytuł roboczy nadany po analizie konkursu (U8) blokował tytuł z pierwszego pytania, bo `chat/route.ts` podmieniał tylko dosłowną „Nową rozmowę". **Zmiana**: pierwsze pytanie nadaje tytuł zawsze (rozmowa bez wiadomości nie ma tytułu od użytkownika — nie ma czego nadpisać); tak samo zachowuje się lista w `ensureConversationId`. **Problem 3 — limit 10 analiz/godzinę wyczerpywał się przy zwykłym testowaniu**: licznik zliczał każdy wpis `ScrapedSource`, także kopie z bazy (U10), które w ogóle nie wychodzą w internet. **Zmiana** w `scrape/route.ts`: wyszukiwanie kopii przeniesione PRZED sprawdzenie limitu — kopia limitu nie zużywa; sam licznik liczy różne adresy (`groupBy` po `rootUrl`) zamiast wpisów. Limit chroni więc dokładnie to, co ma chronić: 10 nowych adresów na godzinę. **Zmiana 4 — świeżość konkursu**: kopia strony konkursu ważna tydzień zamiast 24 h (decyzja właściciela: ogłoszenia konkursowe zmieniają się rzadko). Organizacja bez zmian — kopia bez terminu ważności, odświeżenie przyciskiem „odśwież" przy dymku źródła. **Zmiana 5 — favicon**: domyślny trójkąt Vercela zastąpiony ikoną z dobryai.pl (pomarańczowy `#FF9A6C` kwadrat z białym „AI"); pliki pobrane z żywej strony, bo w lustrze `_public_html/` są tylko odwołania. W `src/app/`: nowy `icon.svg` (główna ikona), podmieniony `favicon.ico` (32×32), nowy `apple-icon.png` (180×180, wyrenderowany z SVG przez `sips` — litery odrobinę cieńsze niż w oryginale, bo systemowy render nie pogrubił Arialu). Nazwy plików rozpoznaje sam Next, `layout.tsx` bez zmian. **Weryfikacja**: `tsc --noEmit` i `npm run build` bez błędów; `npx vitest run` — 73 zielone, czerwony tylko znany guard `webhook.test.ts`. Bez zmian w schemacie bazy, więc bez migracji. **Nieprzetestowane w przeglądarce** — do sprawdzenia przez właściciela: nowa rozmowa → organizacja → konkurs powinny dać jedną pozycję w menu, a pierwsze pytanie zmienić jej tytuł; ikona w karcie przeglądarki po twardym odświeżeniu (`Cmd+Shift+R`).
- [x] Poprawki po code review + wyszukiwarka dokumentacji zamiast czytania całych stron (2026-07-31, commity `a4a8b87`, `8487162`, `3d027f6`; wdrożone na produkcję, backlog: `dokumentacja-aplikacja-granty/19-backlog-optymalizacji.md` zadania 6–8). **Część 1 — siedem poprawek z `POPRAWKI-CODE-REVIEW-2026-07-30.md`** (przegląd commitów `b19cc4e` i `57a7555`): (1) limit 10 analiz/godzinę liczy tylko prawdziwe pobrania z internetu — nowa kolumna `ScrapedSource.wasCrawled` (migracja `20260731090000_scraped_source_was_crawled`), więc kopia z bazy nie zjada puli, a każde „odśwież" i każda nieudana próba liczą się normalnie; poprzednie liczenie różnych adresów (`groupBy` po `rootUrl`) miało dziurę w obie strony; (2) drugi, łagodniejszy limit `MAX_TOTAL_SOURCES_PER_HOUR = 30` obejmuje wszystkie analizy razem z kopiami — kopia nie rusza internetu, ale czyta i zapisuje w bazie wszystkie strony źródła; (3) `robots.ts` zapamiętuje na stałe odpowiedź 4xx (najczęściej 404 — strona nie ma robots.txt), a usuwa z cache tylko błędy chwilowe (timeout, 5xx) — wcześniej crawl 30 stron oznaczał ~30 dodatkowych zapytań o robots.txt; wymagało dodania pola `status` do `SafeFetchError` w `fetch.ts`; (4) wnętrze async IIFE w `robots.ts` opakowane w `try/catch`, żeby odrzucona obietnica nie została w cache na zawsze, plus zabezpieczone `new URL(location, url)` w `fetch.ts` (zepsuty nagłówek `Location` rzucał wyjątek bez `catch`); (5) strumień w `scrape/route.ts` dostał `clientGone` + `cancel()` + bezpieczne `close()` — zamknięcie karty w trakcie crawla nie niszczy już pobrania, strony trafiają do bazy (wzorzec przeniesiony z `chat/route.ts`); (6) `createConversation` w `ChatApp.tsx` jest „single-flight" (ref z obietnicą) — dwa kliknięcia w oknie czasowym przed odpowiedzią serwera nie tworzą już dwóch rozmów; (7) `loadError` blokuje `handleSend` i reużycie rozmowy w `handleSelectSaved` — pusta lista wiadomości przy błędzie sieci nie znaczy „nowa rozmowa" i nie pozwala nadpisać tytułu ani dokleić źródła do rozmowy z historią. **Część 2 — wyszukiwarka i prompt (zadania 6–8 z backlogu)**. Objaw: pytanie o termin ogłoszenia wyników dawało 5 rund narzędzi, ~65 tys. znaków z narzędzi, 19–23 s i za każdym razem `LIMIT` wyczerpanego budżetu. Trzy niezależne przyczyny, wszystkie naprawione: (a) `szukaj_w_dokumentacji` szukała **dosłownego ciągu znaków** (`contains` całej frazy), więc opisowa fraza modelu („wyniki konkursu terminy ogłoszenie") nie trafiała nigdy — cztery wyszukiwania w jednym pytaniu, cztery pudła; teraz `toSearchTerms` rozbija frazę na rdzenie (1 znak ucięty przy krótkich słowach, 2 przy słowach od 7 znaków, minimum 4), z listą słów nieznaczących; (b) o kolejności stron decydowała obecność słów **gdziekolwiek na stronie**, a fragment brany był z **pierwszego** wystąpienia — na czele stawała strona wymieniająca szukane słowa w trzech odległych miejscach, a pokazywany fragment był o czymś innym; teraz `bestCluster` bierze wszystkie wystąpienia i oknem przesuwanym znajduje odcinek o największej liczbie różnych słów, ta sama miara ustawia ranking (`MAX_RANK_CANDIDATES = 10` stron ściąganych z treścią, bo skupienia nie da się ocenić bez treści); (c) **blok systemowy w `chat/route.ts` kazał wprost „przeczytaj właściwe strony, zanim odpowiesz"**, a wyszukiwarkę stawiał tylko jako sposób na znalezienie strony — model wykonywał instrukcję co do joty; kolejność odwrócona (decyzja właściciela), zakaz zgadywania i opierania się na streszczeniu zostawiony bez zmian. Do tego `SEARCH_CONTEXT_CHARS` 300 → 1000 (z fragmentu ma dać się odpowiedzieć) i `alreadyRead` w `DocsToolContext` — treść tej samej strony wysyłana raz na odpowiedź, powtórka dostaje krótką notkę bez odpytywania bazy (w jednym logu ta sama strona poszła dwa razy, 30 292 znaki zmarnowane, 46% budżetu). Zmieniony też `scripts/pomiar-cache.pomiar.ts`: powiela blok systemowy i pętlę narzędziową z trasy, więc bez tej samej zmiany mierzyłby nieaktualny prompt; przy okazji naprawiony wspólny `docsToolContext` dzielony na trzy przebiegi — z nową pamięcią przeczytanych stron drugi i trzeci przebieg dostawałyby „już czytałeś" zamiast treści i skrypt **cicho porównywałby dwie różne rzeczy**. **Pomiary (to samo pytanie, Haiku)**: 5 rund → 1, 65 775 → 5 791 znaków z narzędzi, 18,6–23,5 s → 8,1 s, cache zapis ~50 000 → 7 122, zero odczytów całych stron, `LIMIT` nie wystąpił; potwierdzone na produkcji, zgodne z pomiarem lokalnym (8,6 s). Przy pytaniach o **listę albo zakres** („jakie załączniki", „warunki uczestnictwa") model dalej czyta jedną całą stronę — to zachowanie zaprojektowane, budżet ma zapas (36 i 42 tys. wobec 60 tys.). Trafność trzech odpowiedzi różnego typu potwierdzona przez właściciela, więc wariant awaryjny „zostaw czytanie, podnieś budżet" jest niepotrzebny. Nowa **zasada 5b w `CLAUDE.md`** utrwala kolejność narzędzi i zakaz podnoszenia progów. **Weryfikacja**: `npx tsc --noEmit` bez błędów; `npm test` — 83 zielone (+10 nowych: rdzenie, ranking po skupieniu, fragment nie z pierwszego wystąpienia, deduplikacja odczytów, brak przecieku pamięci między odpowiedziami, ponowna próba po błędzie bazy), czerwony tylko znany guard `webhook.test.ts`. Migracja na produkcji poszła automatycznie (`npx prisma migrate deploy && next build` w ustawieniach Vercela) — potwierdzone w logu builda. **Odkryte przy okazji, nierobione**: funkcje stoją w `iad1`, a produkcyjna baza Neona w `eu-central-1`, więc każde zapytanie leci przez Atlantyk (~1,7 s z 8,1 s w zmierzonym pytaniu) — zapisane w zadaniu 3 backlogu jako korelacja, nie pomiar efektu; przeniesienie bazy albo funkcji dotyka tego, gdzie leżą dane osobowe, więc wymaga decyzji właściciela. Największą pozycją czasową jest teraz 20,9 s ciszy przed pierwszym słowem przy przygotowaniu promptu poniżej 2 s (zadanie 3). Cache bloku systemowego **nie przeżywa przerwy między pytaniami** (trzy pytania z rzędu, wszystkie `odczyt 0` na starcie) — dopisane do zadania 2, ale to jedna sesja, nie rozkład z ruchu.
